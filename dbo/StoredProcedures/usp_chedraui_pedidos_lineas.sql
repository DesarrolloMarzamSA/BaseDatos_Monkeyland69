CREATE PROCEDURE [dbo].[usp_chedraui_pedidos_lineas]
@sucursal INT, @cliente VARCHAR (5), @pedido VARCHAR (16)
WITH ENCRYPTION
AS
BEGIN
--El cuerpo del script estaba cifrado y no se puede reproducir aquí.
    RETURN
END



GO

